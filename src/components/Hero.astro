---
import { heroImage } from "../assets/images";
import { logomark } from "../assets/icons";
import { Image } from "astro:assets";
interface Props {
  title?: string;
  subtitle?: string;
  description?: string;
}

const {
  title = "En drivande kraft genom hela investeringsprojektets alla faser. Resultat börjar med människor",
} = Astro.props;
---

<section id="hero" class="min-h-screen relative">
  <Image
    src={heroImage}
    alt=""
    width={1920}
    height={1080}
    class="absolute inset-0 w-full h-full object-cover -z-1"
  />
  <div class="flex items-end gap-4 pb-16 mr-4 sm:mr-8 min-h-screen pl-4">
    <div class="md:grid grid-cols-[170px_1fr] max-w-full">
      <Image
        src={logomark}
        alt="Pollux Logo"
        width={0}
        height={0}
        class="w-[95px] md:w-[120px] w-24 h-auto md:ml-auto -mt-9 lg:-mt-5.5 flex-shrink-0 mr-4"
      />
      <h1
        class="text-white max-w-full text-[48px] md:text-[72px] lg:text-[96px] mt-2 md:mt-0"
        data-editable="hero_title"
        set:html={title}
      />
    </div>
  </div>
</section>
